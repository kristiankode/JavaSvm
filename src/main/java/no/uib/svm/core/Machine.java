package no.uib.svm.core;

import no.uib.svm.core.dataset.Sample;

/**
 * Main class for SVM
 *
 * Input: set of (input, output) training pair samples; call the
 * input sample features x1, x2…xn, and the output result y.
 * Typically, there can be _lots_ of input features xi
 *
 * Output: set of weights w (or wi), one for each feature,
 * whose linear combination predicts the value of y.
 */
public class Machine {

    /**
     * Trains the machine with a set of data
     */
    public void train(Sample trainingData){

    }

}
